name: Flutter test diagnostics

on:
  pull_request:
    branches:
      - master

permissions:
  contents: read

jobs:
  diagnose-widget-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.44.0'
          channel: stable
          cache: true

      - name: Install dependencies
        run: flutter pub get

      - name: Run widget tests with bounded diagnostics
        shell: bash
        run: |
          set +e
          mkdir -p build/diagnostics
          timeout --signal=TERM 6m \
            flutter test test/widget_test.dart --reporter expanded 2>&1 \
            | tee build/diagnostics/flutter-widget-test.txt
          status=${PIPESTATUS[0]}
          echo "$status" > build/diagnostics/flutter-widget-test-status.txt
          exit 0

      - name: Upload widget test diagnostics
        uses: actions/upload-artifact@v4
        with:
          name: flutter-widget-test-diagnostics
          path: build/diagnostics/
          if-no-files-found: error
          retention-days: 1

      - name: Fail when widget tests failed or timed out
        shell: bash
        run: |
          status="$(cat build/diagnostics/flutter-widget-test-status.txt)"
          if [ "$status" -ne 0 ]; then
            exit "$status"
          fi

package_name("com.ashita_motsumono")

# The credential path is injected by CI. Keep the JSON key outside Git.
json_key_file(ENV["PLAY_SERVICE_ACCOUNT_JSON_PATH"]) if ENV["PLAY_SERVICE_ACCOUNT_JSON_PATH"]

# TODO / 今後の作業

このファイルは、全ソースレビュー後の修正状況と今後の作業をまとめる場所です。

## 今回対応済み

- [x] 確認画面で登録せず戻った場合に、一時Documentと元画像を削除する。
- [x] 通知時刻変更後、登録済みの未完了Todo通知を再予約する。
- [x] チェックリスト項目のON/OFFだけでは通知を再予約しない。
- [x] Drift/SQLite読み込み失敗時にDB退避コピー情報を残す。
- [x] READMEのリリース表記とタグ例を現行版に合わせる。
- [x] Android SDK表記をリリース設定スクリプトと揃える。
- [x] `pubspec.yaml` のアプリバージョンを `0.2.0+1` に更新する。
- [x] 抽出ルールに曜日表現と園・学校向け持ち物辞書を追加する。
- [x] 抽出ロジックのテストを追加する。

## リリース前に確認すること

- [ ] GitHub Actions の `Flutter CI` が通ることを確認する。
- [ ] GitHub Actions の `Release Android` を手動実行し、APK/AAB artifact が生成されることを確認する。
- [ ] Android実機で、カメラ撮影、画像選択、日本語OCR、通知許可、通知予約を確認する。
- [ ] iOS実機またはSimulatorで、画像選択、日本語OCR、通知許可を確認する。
- [ ] 破損DB退避コピーの表示文言がユーザーに分かりやすいか確認する。
- [ ] Google Play ストア掲載文・データセーフティ回答を `docs/STORE_LISTING_JA.md` から転記して確認する。

## 今回対応済み (v0.2.4)

- [x] 「明後日」「翌日」「前日まで」などの相対日付に対応する。
- [x] 「7月10日(火)」「7/10（火）」の曜日付き日付を明示テストする。
- [x] Todo一覧に子ども別フィルターを追加する。
- [x] 完了済みTodoの表示/非表示を切り替えられるようにする。

## 次に改善したい抽出機能

- [x] 「今月末」「月末」「始業式の日」など曖昧な期限を要確認扱いにする。
- [x] 1つの連絡文から「持ち物」「提出」「集金」を複数Todo候補に分割する。
- [x] OCR誤認識補正を追加する。例: O/0、l/1、ー/一、円/￥。
- [x] ユーザーが修正した項目を辞書候補に反映できるようにする。

## 次に改善したいアプリ機能
- [x] 画像なしのOCR貼り付けDocumentを、キャンセル時に確実に削除する回帰テストを追加する。
- [x] DriftStoreの保存/読込/破損退避テストを追加する。
- [x] 設定画面の通知再予約テストを追加する。
- [x] Android release signing と AAB ビルドを追加する。
- [x] プライバシーポリシーを作成する。

## v0.3以降

- [ ] Cloudflare Workers + D1 同期の仕様を再設計する。
- [ ] 同期前提の変更キューをDriftに追加する。
- [ ] 家族共有は需要検証後に実装判断する。
- [ ] 画像/PDF同期はTodo同期より後に検討する。
